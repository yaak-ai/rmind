defaults:
  - /dataset/train-tiny@train.dataset
  - /dataset/val-tiny@val.dataset
  - _self_

_target_: cargpt.datamodules.GenericDataModule
train:
  _target_: torch.utils.data.DataLoader
  shuffle: true
  num_workers: 32
  persistent_workers: true
  multiprocessing_context: forkserver
  pin_memory: false
  collate_fn:
    _target_: yaak_datasets.collate
    _partial_: true

val:
  _target_: torch.utils.data.DataLoader
  shuffle: false
  num_workers: 32
  persistent_workers: true
  multiprocessing_context: forkserver
  pin_memory: false
  collate_fn:
    _target_: yaak_datasets.collate
    _partial_: true
