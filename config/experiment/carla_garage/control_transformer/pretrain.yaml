# @package _global_

defaults:
  - yaak/control_transformer/pretrain
  - override /model: carla_garage/control_transformer/raw
  - override /datamodule: carla_garage/train
  - _self_

paths:
  data: /nasa/drives/carla_garage
