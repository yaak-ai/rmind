# @package _global_

defaults:
  - /paths: default
  - /datamodule: default
  - /trainer: default
  - /vars: control_transformer/default
  - /model: control_transformer/policy_finetune
  - _self_
wandb:
  group: smart
datamodule:
  train:
    batch_size: ${vars.data.train.batch_size}
  val:
    batch_size: ${vars.data.val.batch_size}
