# @package _global_

defaults:
  - /model: yaak/control_transformer/raw
  - /datamodule: yaak/train
  - /trainer: default
  - /trainer/callbacks: pretrain
  - /paths: yaak/default
  - /wandb: yaak/rmind
  - _self_

num_heads: 4
num_layers: 8
encoder_embedding_dim: 384
image_embedding_dim: 384

episode_length: 6
action_horizon: 1
episode_step: 10
episode_stride: 10
# clip_horizon is the dataset build horizon (max over PT/FT) so the built dataset
# is shared: PT slices it to immediate actions (action_horizon=1), FT to chunks.
clip_horizon: 6
clip_length: "${eval:'${episode_length} + ${clip_horizon} - 1'}"
clip_period: "${eval:'${clip_length} * ${episode_step}'}i"

# Keep percentage for static at-rest brake-hold windows (samples_cast cap in
# dataset/yaak/train.yaml); 100 = no cap (natural distribution).
atrest_keep_pct: 100

speed_bins: 512
gas_pedal_bins: 255
brake_pedal_bins: 165
steering_angle_bins: 961
