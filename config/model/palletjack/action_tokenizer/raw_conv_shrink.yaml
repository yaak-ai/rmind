---
# `raw_conv.yaml` plus the fork1 soft-threshold: the two arms that each moved fork on
# their own, combined.
#
# b2 (conv) took fork EV +0.817 -> +0.847 and fork TV 1.324 -> 1.092 at 32 bits, the
# best fork of any arm at that rate. b5 (shrinkage) took the exact-zero share 0.0% ->
# 55.2% against a target that is 92.4% zeros, with fork TV 1.324 -> 1.233. They address
# different halves of the same axis -- the conv represents the edges, the shrinkage
# represents the silence between them -- so they should compose.
_target_: rmind.models.action_tokenizer.ActionTokenizer
_recursive_: false
_convert_: all

input_transform:
  _target_: torch.nn.Sequential
  _convert_: all
  _args_:
    - _target_: rmind.components.nn.Remapper
      paths:
        continuous:
          traction: [data, traction]
          steering: [data, steering]
          fork1: [data, fork1]
    - _target_: rmind.components.containers.ModuleDict
      modules:
        continuous:
          _target_: rmind.components.nn.Identity

encoder:
  _target_: rmind.components.nn.ChunkConvEncoder
  num_steps: ${action_clip}
  num_axes: ${action_space}
  out_features: ${action_latent_dim}
  channels: ${conv_channels}
  dilations: [1, 3, 9]

quantizer:
  _target_: rmind.components.vq.ResidualVQ
  dim: ${action_latent_dim}
  codebook_size: ${codebook_size}
  num_quantizers: ${num_quantizers}

decoder:
  _target_: torch.nn.Sequential
  _args_:
    - _target_: rmind.components.nn.ChunkConvDecoder
      num_steps: ${action_clip}
      num_axes: ${action_space}
      in_features: ${action_latent_dim}
      channels: ${conv_channels}
      dilations: [9, 3, 1]
    # LAST: fork1's 91.7% zero atom needs to decode to EXACTLY zero, which a
    # continuous head cannot do -- see `nn.AxisShrinkage`
    - _target_: rmind.components.nn.AxisShrinkage
      num_steps: ${action_clip}
      num_axes: ${action_space}
      axes: [2]
      init_threshold: 0.05

targets:
  continuous:
    traction: [continuous, traction]
    steering: [continuous, steering]
    fork1: [continuous, fork1]

commitment_weight: 1.0
vq_weight: 5.0

optimizer:
  _target_: rmind.components.optimizers.SelectiveAdamW
  _recursive_: true
  lr: 5.5e-5
  betas: [0.9, 0.95]
  weight_decay: 0.1
  weight_decay_module_blacklist:
    - _target_: hydra.utils.get_class
      path: torch.nn.Embedding
    - _target_: hydra.utils.get_class
      path: torch.nn.LayerNorm

lr_scheduler:
  interval: step
  scheduler:
    _target_: rmind.components.lr_schedulers.get_cosine_schedule_with_warmup
    num_warmup_steps: ${lr_warmup_steps}
    num_training_steps: ${lr_total_steps}
