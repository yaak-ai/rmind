---
# `raw_dct.yaml` plus a learned soft-threshold on the fork1 axis.
#
# WHY. fork1's reconstruction is not too smooth -- it is too JAGGED. Total variation of
# the reconstruction over ground truth runs 1.09-1.39 on fork1 across every arm, against
# 0.40-0.57 on traction, which genuinely is over-smoothed. Ground truth fork1 is exactly
# 0.000 for 91.7% of messages and a continuous head cannot emit exact zeros, so it
# sprays low-amplitude noise over the quiet stretches while attenuating the real events.
#
# Rate will not fix it: fork1's TV ratio RISES with rate (1.02 at 16 bits, 1.39 at 32),
# so extra capacity buys more wiggle. It is the output family that cannot represent the
# spike at zero, which is why this is a parameterization arm and not another rate arm.
#
# See `rmind.components.nn.AxisShrinkage`. Only fork1 is shrunk -- traction and steering
# have no zero atom, and shrinking them would just bias them toward zero.
_target_: rmind.models.action_tokenizer.ActionTokenizer
_recursive_: false
_convert_: all

input_transform:
  _target_: torch.nn.Sequential
  _convert_: all
  _args_:
    - _target_: rmind.components.nn.Remapper
      paths:
        continuous:
          traction: [data, traction]
          steering: [data, steering]
          fork1: [data, fork1]
    - _target_: rmind.components.containers.ModuleDict
      modules:
        continuous:
          _target_: rmind.components.nn.Identity

encoder:
  _target_: torch.nn.Sequential
  _args_:
    # fixed orthonormal DCT-II, then the SAME MLP shape as `raw.yaml`
    - _target_: rmind.components.nn.ChunkDCT
      num_steps: ${action_clip}
      num_axes: ${action_space}
      num_coefficients: ${dct_coefficients}
    - _target_: rmind.components.nn.Linear
      in_features: ${action_coeff_dim}
      out_features: ${action_hidden_dim}
    - _target_: torch.nn.GELU
    - _target_: rmind.components.nn.Linear
      in_features: ${action_hidden_dim}
      out_features: ${action_latent_dim}

quantizer:
  _target_: rmind.components.vq.ResidualVQ
  dim: ${action_latent_dim}
  codebook_size: ${codebook_size}
  num_quantizers: ${num_quantizers}

decoder:
  _target_: torch.nn.Sequential
  _args_:
    - _target_: rmind.components.nn.Linear
      in_features: ${action_latent_dim}
      out_features: ${action_hidden_dim}
    - _target_: torch.nn.GELU
    - _target_: rmind.components.nn.Linear
      in_features: ${action_hidden_dim}
      out_features: ${action_coeff_dim}
    # LAST, so the reconstruction loss is still taken in the TIME domain -- scoring in
    # coefficient space would weight error per-coefficient rather than per-sample and
    # the fork1 event weighting would stop meaning anything
    - _target_: rmind.components.nn.ChunkIDCT
      num_steps: ${action_clip}
      num_axes: ${action_space}
      num_coefficients: ${dct_coefficients}
    # LAST: soft-threshold fork1 so its 91.7% zero atom decodes to EXACTLY zero
    - _target_: rmind.components.nn.AxisShrinkage
      num_steps: ${action_clip}
      num_axes: ${action_space}
      # index 2 in the (traction, steering, fork1) order `_gather_actions` stacks
      axes: [2]
      init_threshold: 0.05

targets:
  continuous:
    traction: [continuous, traction]
    steering: [continuous, steering]
    fork1: [continuous, fork1]

commitment_weight: 1.0
vq_weight: 5.0

optimizer:
  _target_: rmind.components.optimizers.SelectiveAdamW
  _recursive_: true
  lr: 5.5e-5
  betas: [0.9, 0.95]
  weight_decay: 0.1
  weight_decay_module_blacklist:
    - _target_: hydra.utils.get_class
      path: torch.nn.Embedding
    - _target_: hydra.utils.get_class
      path: torch.nn.LayerNorm

lr_scheduler:
  interval: step
  scheduler:
    _target_: rmind.components.lr_schedulers.get_cosine_schedule_with_warmup
    num_warmup_steps: ${lr_warmup_steps}
    num_training_steps: ${lr_total_steps}
