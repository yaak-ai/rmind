---
# finetune callbacks + EMA over the trainable (policy) params.
# Val metrics are computed with the EMA weights swapped in; checkpoints keep
# raw weights in state_dict and the EMA shadow under callback state.
- _target_: rmind.callbacks.LogitBiasSetter
- _target_: rmind.callbacks.ModuleFreezer
  paths:
    - episode_builder
    - encoder
- _target_: rmind.callbacks.EMAWeights
  decay: ${oc.select:flow_ema_decay,0.999}
- _target_: pytorch_lightning.callbacks.ModelSummary
  max_depth: 5
- _target_: pytorch_lightning.callbacks.ModelCheckpoint
  every_n_epochs: 1
  save_on_train_epoch_end: True
- _target_: pytorch_lightning.callbacks.LearningRateMonitor
  logging_interval: step
- _target_: rmind.callbacks.WandbAttentionMaskLogger
